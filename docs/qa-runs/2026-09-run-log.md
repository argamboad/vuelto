# QA Run Log — 2026-09 app-slice pass (P11)

> Mirrors the official run-log structure (`docs/QA_RUN_LOG.pdf` / `gen_qa_runlog.py`) for the **app
> sections §10d–§10n** of `docs/QA_TEST_PLAN.md` — the surface the port added (P1–P10b). The platform
> sections (§4–§10c, §11–§14b) were passed on the template in the 2026-08 run (`2026-08-run-log.md`) and
> are unchanged by the port apart from the rebrand; they are not re-run here. **The three IdP/provider-bound
> cases are left blank** for the owner (a live rate key, a live mail consent, a real voucher inbox).

| | |
|---|---|
| **Testers** | Claude (driver) + Allan Gamboa (owner — the blank rows) |
| **Pass date** | 2026-09-04 |
| **Environment** | A (local): develop @ PR #17 merge (`f67eb1b`) · API `https:7160` · Web `https:7008` · Postgres + Mailpit via the `vuelto` compose stack · no `ExchangeRate__ApiKey`, no OAuth apps (fresh checkout posture) |
| **Method** | API halves: `scratchpad/p11_qa_api.py` — every §10d–§10n Postman/API assertion replayed against **fresh households** (OTP-minted tokens; the Spanish-seed case with `PUT /api/auth/locale es` before the first catalog read) → **114 assertions, 0 failures**. Web halves: driven in the owner's Chrome on the owner's household in **English and Spanish** (full page tour for the localization sweep), plus each slice's merge-time walkthrough (P7–P10b, this repo's PR bodies). |

**Legend:** ✅ Pass (date noted) · ⬜ pending (owner — needs a credential) · ⚙️ also automated in CI ·
🔑 needs an OAuth/provider hand-off · 👁 visual judgment

**Sweep result (localization):** 566 keys, EN ↔ ES parity (CI gate), zero missing keys (new
`LocalizationKeyCoverageTests` gate), zero raw keys on any ported page in Spanish. Seeded catalog names stay
in the household's first-read language (by design, ADR-V009).

**Plan correction found by the pass:** QA-LED-05 said an 80,000 edit makes a 30 % refund read ₡40,000; the
app (correctly) computes ₡24,000 — the plan text was fixed in this run.

---

## §10d Web — Budget settings (BUDGET-1)

| Case | Title | Result | Date | Notes |
|---|---|---|---|---|
| QA-BUD-01 | A new household sees the budget defaults ⚙️ | ✅ | 2026-09-04 | fresh household: `is_default true`, Thursday / last Thursday prev / 0 USD; Settings card renders the same (EN + ES) |
| QA-BUD-02 | Save the household's budget structure and see it persist ⚙️ | ✅ | 2026-09-04 | PUT Monday / 1st of month / 1500·1800 USD / 400000·500000 CRC → GET persists, `is_default false`; re-save another weekday → one row |
| QA-BUD-03 | Invalid values are refused and nothing changes ⚙️ | ✅ | 2026-09-04 | weekday 9, anchor x, −5, EUR → 400 `invalid_request`; GET unchanged; no token → 401 |

## §10e Web — Catalog: categories & banks (CATALOG-1/2)

| Case | Title | Result | Date | Notes |
|---|---|---|---|---|
| QA-CAT-01 | First visit seeds the defaults in the household's language ⚙️ | ✅ | 2026-09-04 | Spanish-locale user → 7 categories (Alimentación … Otro), 9 banks incl. Efectivo; switching to `en` leaves the names Spanish (seeded once). Note: the seed follows the JWT `locale` claim, not `Accept-Language` |
| QA-CAT-02 | Create a category; duplicate refused case-insensitively ⚙️ | ✅ | 2026-09-04 | Viajes 201 → VIAJES 409 `category_exists` (no reactivate offer) |
| QA-CAT-03 | Deactivate, then the inactive clash offers Reactivate ⚙️ | ✅ | 2026-09-04 | viajes → 409 `category_exists_inactive` + `existing_id`/`existing_name`; reactivate keeps "Viajes" |
| QA-CAT-04 | Banks behave the same and are household-wide ⚙️ | ✅ | 2026-09-04 | Lafise → Lafise CR, Coopenae 201, PUT another household's bank id → 404 |

## §10f Web — Exchange rate (FX-1)

| Case | Title | Result | Date | Notes |
|---|---|---|---|---|
| QA-FX-01 | Home shows today's live rate 🔑 | ⬜ | | needs `ExchangeRate__ApiKey` — the owner enters credentials himself |
| QA-FX-02 | No provider → the honest "unavailable" state ⚙️ | ✅ | 2026-09-04 | fresh household, no key, no transactions → 503 `exchange_rate_unavailable`; New transaction shows the red "enter one to continue" hint (EN + ES) |

## §10g Web — Envelopes (ENV-1)

| Case | Title | Result | Date | Notes |
|---|---|---|---|---|
| QA-ENV-01 | Create with targets + reminder; duplicates refused ⚙️ | ✅ | 2026-09-04 | Marchamo ₡718,000 five_week_months 201; MARCHAMO 409 `envelope_exists`; cadence `weekly` 400 naming `reminder_cadence`; negative 400 |
| QA-ENV-02 | Edit, deactivate, inactive clash restores ⚙️ | ✅ | 2026-09-04 | 750000 inactive; marchamo → 409 `envelope_exists_inactive`; reactivate ₡800,000 keeps "Marchamo"; foreign id → 404 |

## §10h Web — Months & transactions (LEDGER-1/2/3)

| Case | Title | Result | Date | Notes |
|---|---|---|---|---|
| QA-LED-01 | First transaction creates its month with weeks + income snapshot ⚙️ | ✅ | 2026-09-04 | 2026-07-10 → July 2026, 5 weeks 25 Jun–29 Jul, 3750 USD / 312500 CRC; resolve 2026-05-30 → June `is_new` |
| QA-LED-02 | Edit keeps the frozen rate; a date fix moves the transaction ⚙️ | ✅ | 2026-09-04 | 100000 → $200 at 500; date 2026-06-05 → June (4 weeks), July auto-deleted (404) |
| QA-LED-03 | Deleting the last transaction removes the month ⚙️ | ✅ | 2026-09-04 | 204 → GET month 404, months empty |
| QA-LED-04 | Month income editable; invalid + foreign refused ⚙️ | ✅ | 2026-09-04 | 1600000 CRC persists; −1 / EUR → 400; foreign month → 404; blank payee → 400 naming the field |
| QA-LED-05 | Unplanned essential with an expected refund that follows ⚙️ | ✅ | 2026-09-04 | 50000·30 % → ₡15,000 pending; 80000 → ₡24,000; switch off → gone; 150 % → 400 (plan text corrected) |
| QA-LED-06 | Mark received books an inflow; revert removes it ⚙️ | ✅ | 2026-09-04 | inflow `refund_realization` ₡15,000; DELETE inflow → 400 `derived_transaction`; received twice idempotent (one inflow); pending → gone; foreign → 404; UI: Month list showed the inflow row read-only (P10b walkthrough) |

## §10i Web — Budget lines (EXPENSES-1)

| Case | Title | Result | Date | Notes |
|---|---|---|---|---|
| QA-EXP-01 | Create fixed + variable; single-currency + category rules ⚙️ | ✅ | 2026-09-04 | Mortgage ₡300,000 BAC bank_account; Netflix $13 unassigned; Rent on the same category → 400; both currencies → 400 |
| QA-EXP-02 | Reorder; inactive lines out of the order ⚙️ | ✅ | 2026-09-04 | reorder 204 + persisted; Water inactive; reorder with an inactive id → 400 |
| QA-EXP-03 | Duplicates per list; inactive clash restores; foreign 404 ⚙️ | ✅ | 2026-09-04 | MORTGAGE 409 `expense_exists`; variable Mortgage 201; water → 409 `expense_exists_inactive`; reactivate ₡20,000; foreign → 404 |

## §10j Web — Dashboard (DASH-1)

| Case | Title | Result | Date | Notes |
|---|---|---|---|---|
| QA-DASH-01 | Reflects income, lines vs actuals, other spending, balance ⚙️ | ✅ | 2026-09-04 | summary: Mortgage budget/actual ₡300,000, unplanned ₡50,000, bank×method BAC/bank_account; rate from the last-transaction tier. Web: banner + cards rendered EN/ES (P7/P10b walkthroughs) |
| QA-DASH-02 | Selector, entry points, empty state, blocked projections ⚙️ | ✅ | 2026-09-04 | unknown month → 404; anonymous → 401; empty state "Nothing to show yet" / "Aún no hay nada que mostrar" in Chrome |

## §10k Web — Reports (REPORTS-1/2)

| Case | Title | Result | Date | Notes |
|---|---|---|---|---|
| QA-REP-01 | Category analysis by month shows budgets; a range doesn't ⚙️ | ✅ | 2026-09-04 | month → `single_month true` + ₡300,000 budget; range → false; no period → 400 `period_required`; from > to → 400 `period_invalid` |
| QA-REP-02 | Export CSV downloads the shown period; fixed columns, 4-decimal rate ⚙️ | ✅ | 2026-09-04 | export → `download_url` (relative `api/files/{token}`), anonymous GET → 200 `attachment`, header exact, `500.0000`, newest first; foreign month → 404. Browser download verified in the P8 walkthrough |

## §10l–§10n Web — Email ingestion (EMAIL-2…6)

| Case | Title | Result | Date | Notes |
|---|---|---|---|---|
| QA-EMAIL-01 | Email settings without credentials: honest errors ⚙️ | ✅ | 2026-09-04 | empty list; authorize → 400 `provider_not_configured` / `invalid_provider`; POST → 400 `use_consent_flow`; presets BAC + BN; page renders EN/ES with both Connect buttons |
| QA-EMAIL-02 | Live consent round-trip 🔑 | ⬜ | | needs the OAuth apps + the API callback URI registered (owner) |
| QA-EMAIL-03 | Edit with real folders, backfill rule, disconnect, no cross-user leak | ✅ | 2026-09-04 | uniform 404 on get/folders/sync/delete of unknown ids (API); edit/backfill/disconnect UI proven in the P9b walkthrough (import-from fix) |
| QA-EMAIL-04 | Sync now stages drafts once; a dead inbox says so 🔑 | ⬜ | | needs a live inbox with voucher mail; the dead-inbox half ("Reconnect this inbox") passed in the P10a walkthrough |
| QA-EMAIL-05 | Merchant rules suggest, never apply, unique per household ⚙️ | ✅ | 2026-09-04 | 201 / 409 `mapping_exists` / 400 class / update / 404 (API); Manage suggestions add + dupe refused + Suggested badge on the queue (P10b walkthrough) |
| QA-EMAIL-06 | Review queue: confirm books once, discard never reverts ⚙️ | ✅ | 2026-09-04 | empty queue + count 0 + unknown → 404 (API); confirm/discard/badge/banner/no-rate refusal proven in the P10b walkthrough; concurrency by `PendingVoucherSliceTests` |

---

**Summary:** 29 of 32 app cases ✅, 3 ⬜ (owner credentials). No open findings. Test data lives in five
throwaway households (`qa-p11-*@test.local`) in the local dev DB — gone with the next wipe.
