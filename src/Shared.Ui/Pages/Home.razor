@page "/"
@using Vuelto.Shared.Ui.Auth
@inject AuthService Auth
@inject IStringLocalizer<AppStrings> Localizer

@* The root: signed in, it IS the dashboard (owner decision 2026-09-04 — the month at a glance is what you
   open the app for, so no welcome card in between); signed out, the public hero with the sign-in call.
   /dashboard and /dashboard/{id} keep working as the same page (deep links, the month page's link). *@
@if (Auth.IsAuthenticated)
{
    <Dashboard />
}
else
{
    <PageTitle>@Localizer["Nav_Home"]</PageTitle>
    <div class="welcome-wrapper">
        <div class="hero-section text-center">
            <img src="_content/Vuelto.Shared.Ui/brand/lockup_light_1520.png" alt="¿Y el vuelto?"
                 class="welcome-lockup mb-4" />
            <p class="lead text-muted mb-4 mx-auto" style="max-width:480px">
                @Localizer["Home_HeroSubtitle"]
            </p>
            <a href="/login" class="btn btn-primary btn-lg px-5">@Localizer["Home_SignInCta"] &rarr;</a>
        </div>
    </div>
}
