<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true" android:networkSecurityConfig="@xml/network_security_config"></application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 11+ package visibility: without these, WebAuthenticator can't see any browser
         (logcat shows "AppsFilter: interaction ... BLOCKED") and falls back from a Custom Tab to
         a full external browser. In a full browser the app is backgrounded and Android may kill
         the process mid-OAuth; the perezosoft:// callback then cold-starts a process with no
         pending auth operation and the app just closes. Custom Tabs keep the app task foreground
         for the whole round trip. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>
</manifest>